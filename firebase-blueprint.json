{
  "entities": {
    "Project": {
      "title": "Project",
      "description": "A Canva project link for the stationery shop.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the project (e.g., Business Card, Banner)."
        },
        "canvaUrl": {
          "type": "string",
          "format": "uri",
          "description": "The Canva sharing link."
        },
        "code": {
          "type": "string",
          "description": "A unique 6-character alphanumeric code for the project."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this project link."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the project was added."
        }
      },
      "required": ["name", "canvaUrl", "userId", "createdAt"]
    }
  },
  "firestore": {
    "/projects/{projectId}": {
      "schema": "Project",
      "description": "Collection of Canva project links."
    }
  }
}
